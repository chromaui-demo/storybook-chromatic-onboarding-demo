import { Meta } from '@storybook/addon-docs/blocks';
import { CoursePage } from '../../packages/hub/src/onboarding/CoursePage';

<Meta title="Onboarding/Reference/Facilitator setup" />

<CoursePage
  phase="Reference"
  title="Prepare the lab before SEs arrive"
  description="Set up the repository, accounts, Chromatic project, and support path so each SE can work independently without exposing secrets."
>

An SE should be able to start without waiting for a meeting. Complete this work before you assign the
setup page.

## Learner access

Confirm each SE has:

- an approved computer with permission to install VS Code, Git, Node.js, pnpm, and Codex;
- a ChatGPT account with Codex access;
- read and write access to the GitHub repository;
- access to the Chromatic organization and training project;
- access to the approved secret manager and support channel.

## Repository readiness

1. Publish this repository with a protected `main` branch and a passing initial commit.
2. Run `pnpm test:onboarding:setup` on Node 20 to test a clean clone, install, branch, checks, Hub, and React Storybook.
3. Decide whether SEs may merge course branches or whether branches remain disposable.
4. Give every SE write access and reserve the `onboarding/<handle>` branch namespace.

The checked-in repository already pins Storybook 10.5.5. Day 1 therefore performs a real upgrade
health check and migration review, but the upgrade command should be a no-op. If a cohort must perform
a live Storybook 9-to-10 migration, maintain a tested `course/storybook-9-start` tag separately and
have every SE branch from that tag. Do not downgrade shared `main` during a cohort.

## Shared Chromatic project

1. Create or select one Chromatic project for `packages/react` in the demo organization.
2. Connect the GitHub repository and install the Chromatic GitHub App.
3. Publish and accept a clean `main` build before SEs branch. Keep `main` passing throughout the
   cohort so baseline ancestry remains trustworthy.
4. Store `CHROMATIC_PROJECT_TOKEN` as a GitHub repository secret. Share it locally only through the
   team's approved secret manager; never paste it into course docs, chat transcripts, or source.
5. Give SEs the least role needed for the exercise. Use Reviewer for assigned peer review and
   Developer only when SEs must accept their own UI Test baselines.
6. Fill in and share these values through the approved internal channel:

| Setting                | Value       |
| :--------------------- | :---------- |
| Chromatic organization | `<fill in>` |
| Chromatic project      | `<fill in>` |
| Project URL            | `<fill in>` |
| Support channel        | `<fill in>` |
| Course owner           | `<fill in>` |
| Demo reviewer          | `<fill in>` |

## Cohort guardrails

- One SE, one namespaced Git branch, one corresponding Chromatic branch.
- SEs review only their own newest branch build unless they are assigned as a reviewer.
- Do not enable automatic acceptance on SE branches.
- Do not enable TurboSnap until the project has enough successful full builds and the SE can
  explain the dependency graph.
- Archive or delete stale course branches only after progress and feedback have been captured.

## Facilitator checkpoints

Review progress after Days 5, 10, and 15. Let the SE drive the product and explain each decision.
Ask what the customer gains or risks. Do not grade only on whether the SE remembered a flag.

</CoursePage>
