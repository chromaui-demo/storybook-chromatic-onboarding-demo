import { Meta } from '@storybook/addon-docs/blocks';
import { CoursePage } from '../../../packages/hub/src/onboarding/CoursePage';

<Meta title="Onboarding/Post-sales technical training/Core/06 Storybook Test locally" />

<CoursePage
  phase="Core"
  day={6}
  duration="60–90 minutes"
  title="Run Storybook Test locally"
  description="Turn the reservation stories into browser tests, debug them beside the component, and verify what a guest can see and do."
  customerProblem="A static screenshot cannot prove that a guest can choose a date and complete a reservation."
  outcome="You can run Storybook Test locally and write, debug, and explain a focused play function that verifies customer-visible behavior."
  previous={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-05-mock-service-worker--docs', label: 'Day 5 · Mock Service Worker' }}
  next={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-07-accessibility--docs', label: 'Day 7 · Accessibility' }}
>

## Plan your session

- 15 minutes: read
- 35–50 minutes: write and debug tests
- 15 minutes: run and explain

<aside className="learning-callout learning-callout--why">
  <strong>Why this day matters</strong>
  <div>
    Storybook Test keeps customer-visible behavior beside the state that defines
    it. The same place helps developers understand, debug, and automate the
    test.
  </div>
</aside>

## Understand Storybook Test

- [Testing with Storybook](https://storybook.js.org/docs/writing-tests)
- [Interaction testing](https://storybook.js.org/docs/writing-tests/interaction-testing)
- [Vitest addon](https://storybook.js.org/docs/writing-tests/integrations/vitest-addon)

Storybook Test uses the Vitest addon to turn stories into browser tests. Every story can prove that a
state renders. A play function can also prove customer-visible behavior. The testing widget keeps the
test result and debugging steps beside the rendered state.

## Test the reservation like a user

1. Run `ReservationCard / Reservation interaction` in the Interactions panel. Step through its
   `play` function and connect each log entry to the source.
2. Add an interaction to an `AvailabilityPanel` story: wait for mocked dates, choose a date, and assert
   the visible selected state or callback payload.
3. Query by accessible role, name, or label. Use `data-testid` only after documenting why a user-facing
   query cannot express the behavior.
4. Await every `userEvent` and assertion. Use `fn()` for callback args rather than a logging-only action.
5. Run the React story tests in headless Chromium:

   ```sh
   pnpm --filter @demo/react test:storybook
   ```

6. Introduce one temporary assertion failure, inspect it in Storybook and the terminal, then restore
   the correct behavior. Compare which surface made diagnosis faster and why.

## Confirm the failure and the fix

- The `play` function fails for the intended bad assertion and passes after the fix.
- The test asserts a customer-visible outcome, not implementation detail.
- Type checking, story tests, and the static build pass.

## Questions to think through

- Which behavior matters enough to test at the component level?
- Which query best matches what the user can perceive?
- What does the visible Storybook failure reveal that terminal output does not?
- Which behavior still belongs in an end-to-end test?

## How would you respond?

> “Why add an interaction test here if we already test the full reservation flow?”

Explain the testing layers: every story gets a render test; important behavior gets an interaction
test; broad appearance is covered by visual tests; narrow logic may still deserve unit tests.

<details>
<summary>Hint</summary>

Chromatic waits for a story's full `play` function before capturing the final state. If the customer
needs visual coverage at multiple intermediate points, create distinct stories for those states rather
than expecting a screenshot after every step.

</details>

## Save the interaction test

```sh
git add packages/react
git commit -m "onboarding(day-06): test the reservation interaction"
```

</CoursePage>
