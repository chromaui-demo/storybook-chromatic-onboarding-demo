import { Meta } from '@storybook/addon-docs/blocks';
import { CoursePage } from '../../../packages/hub/src/onboarding/CoursePage';

<Meta title="Onboarding/Post-sales technical training/Core/02 Agentic setup" />

<CoursePage
  phase="Core"
  day={2}
  duration="75–90 minutes"
  title="Use Codex without giving up review"
  description="Give Codex one bounded Storybook task, inspect the result, and keep only the changes you understand."
  customerProblem="The design-system team wants AI-assisted setup, but it cannot trust unreviewed configuration or tests."
  outcome="You can scope an AI-assisted task and explain every package, configuration change, story, and test you keep."
  previous={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-01-upgrade-orient--docs', label: 'Day 1 · Upgrade & orient' }}
  next={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-03-stories-docs-manifests--docs', label: 'Day 3 · Stories & manifests' }}
>

## Plan your session

- 10 minutes: read
- 45–60 minutes: prompt, inspect, and refine
- 15 minutes: verify and explain

<aside className="learning-callout learning-callout--why">
  <strong>Why this day matters</strong>
  <div>
    AI can shorten setup work, but the engineer still owns scope, review, and
    verification. Customer trust depends on that ownership.
  </div>
</aside>

## Understand the supported workflow

- [Agentic setup](https://storybook.js.org/docs/10.5/ai/setup)
- [Storybook installation](https://storybook.js.org/docs/10.5/get-started/install)

Agentic setup is a preview capability for React with the Vite builder. It can accelerate analysis,
configuration, mocks, stories, play functions, verification, and MCP installation. You still own every
change.

## Give Codex a bounded job

1. Open Codex at the repository root. The official Storybook documentation proposes:

   ```text
   Set up Storybook for me with npm create storybook@latest and follow its instructions precisely
   ```

2. For this pinned pnpm course, use the constrained version below so Codex does not switch package
   managers, change sibling frameworks, or silently move beyond Storybook 10.5:

   ```text
   Review and finish setting up Storybook for packages/react using React, Vite, pnpm, and Storybook
   10.5.5. Follow Storybook's official agentic setup instructions precisely. Keep every Storybook
   package on 10.5.5, do not edit sibling packages, preserve the existing customer scenario, tag new
   stories ai-generated, and stop to explain any destructive or cross-workspace change before making it.
   Run the React Storybook tests, type checker, and static build when finished.
   ```

3. Ask Codex for a setup plan before it edits. Compare that plan with the existing
   `main.ts`, `preview.tsx`, `vitest.config.ts`, stories, and package scripts.
4. Let Codex improve one bounded area, such as missing component JSDoc, a portal root, or a story state.
   Reject unrelated rewrites.
5. Review every changed line. Keep `ai-generated` tags until Day 3 validates the stories.

## Review the diff as the owner

```sh
pnpm --filter @demo/react typecheck
pnpm --filter @demo/react exec vitest run --project=storybook
pnpm --filter @demo/react build-storybook
git diff --check
```

Discuss one useful Codex observation, one suggestion you rejected or constrained, and why the final
change is trustworthy. Keep notes in your preferred system if they will help later.

## Questions to think through

- Which parts of this task are safe to delegate to an agent?
- Which changes require the strongest human review?
- What would make you stop or narrow the task?
- Which test result proves the agent's change is safe?

## How would you respond?

Put this in your own words: **Where does Storybook AI save time, and where does engineering judgment
still matter?**

<details>
<summary>Recovery</summary>

If Codex tries to initialize another Storybook or change package management, stop it. Revert only those
unrelated edits. Narrow the prompt to `packages/react` and ask for one proposed change at a time.

</details>

## Save what you trust

```sh
git add packages/react
git commit -m "onboarding(day-02): review agentic Storybook setup"
```

</CoursePage>
