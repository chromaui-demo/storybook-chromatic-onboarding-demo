import { Meta } from '@storybook/addon-docs/blocks';
import { CoursePage } from '../../../packages/hub/src/onboarding/CoursePage';

<Meta title="Onboarding/Post-sales technical training/Core/05 Mock Service Worker" />

<CoursePage
  phase="Core"
  day={5}
  duration="75–90 minutes"
  title="Make API states repeatable"
  description="Put loading, success, empty, and error states one click away without depending on a live backend."
  customerProblem="Network-dependent components create flaky demos and tests. Important error states are difficult to reproduce."
  outcome="You can use MSW to reproduce customer-relevant states and keep those states stable in development and tests."
  previous={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-04-storybook-mcp--docs', label: 'Day 4 · Storybook MCP' }}
  next={{ href: './?path=/docs/onboarding-post-sales-technical-training-core-06-storybook-test-locally--docs', label: 'Day 6 · Storybook Test locally' }}
>

## Plan your session

- 10 minutes: read
- 20 minutes: install and configure
- 40–50 minutes: build states
- 10 minutes: verify and explain

<aside className="learning-callout learning-callout--why">
  <strong>Why this day matters</strong>
  <div>
    Repeatable network states make rare errors and empty results easy to see,
    test, and discuss without depending on a live backend.
  </div>
</aside>

## Understand the network boundary

- [Mocking network requests](https://storybook.js.org/docs/10.5/writing-stories/mocking-data-and-modules/mocking-network-requests)
- [Storybook loaders](https://storybook.js.org/docs/10.5/writing-stories/loaders)

## Make every response state repeatable

1. Install MSW in the React workspace and generate its worker:

   ```sh
   pnpm --filter @demo/react add -D msw msw-storybook-addon
   pnpm --filter @demo/react exec msw init public --save
   ```

2. Expose `packages/react/public` through `staticDirs` in `.storybook/main.ts`. Initialize MSW and add
   its loader in `.storybook/preview.tsx` following the current official guide.
3. Create `AvailabilityPanel.tsx`. It should request
   `GET /api/properties/:propertyId/availability` and render a stable loading state followed by
   available dates, an empty result, or an error message.
4. Create stories for `LoadingThenAvailable`, `NoAvailability`, and `ServiceError`. Put default handlers
   at the narrowest level that avoids duplication, then override them per story.
5. Ensure every request is intercepted. An unhandled request should be treated as a test failure or a
   deliberate, documented exception.
6. Run Storybook with the browser network panel open and prove that no real service is contacted.

## Confirm every response state

- Refreshing each story produces the same state.
- Success, empty, and error paths can be opened directly by story URL.
- The service worker is served by Storybook, but generated build output remains untracked.
- Type checking, story tests, and the static build pass.

## Questions to think through

- Which backend states are hardest for the team to reproduce?
- Which mock belongs at the project, component, or story level?
- How would an unhandled request weaken the test?
- When should the team still use a real integration environment?

## How would you respond?

Try the customer version: **Why is an API mock part of a trustworthy visual-testing strategy instead
of just a local development convenience?**

<details>
<summary>Hint</summary>

Avoid fixed waits in tests. Make the component expose accessible status text and let later interaction
tests wait for the state the user would observe.

</details>

## Save the fixtures

```sh
git add packages/react pnpm-lock.yaml
git commit -m "onboarding(day-05): add deterministic MSW scenarios"
```

</CoursePage>
