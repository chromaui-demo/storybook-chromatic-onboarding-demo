import { Meta } from '@storybook/addon-docs/blocks';
import { CoursePage } from '../../../packages/hub/src/onboarding/CoursePage';

<Meta title="Onboarding/Core/10 CI & core demo" />

<CoursePage
  phase="Core"
  day={10}
  duration="75–90 minutes"
  title="Put Chromatic in CI, then tell the story"
  description="Wire the workflow to GitHub Actions and give a five-minute demo without touring every tab."
  customerProblem="Visual testing depends on a developer remembering to publish locally, so branch coverage and review checks are inconsistent."
  outcome="You can trace a commit through CI and give the short version of the Storybook and Chromatic workflow."
  previous={{ href: './?path=/docs/onboarding-core-09-visual-review--docs', label: 'Day 9 · Visual review' }}
  next={{ href: './?path=/docs/onboarding-advanced-11-modes--docs', label: 'Day 11 · Modes' }}
>

## Give this 90 minutes

- 15 minutes: read
- 25–35 minutes: configure or audit CI
- 20 minutes: verify the branch run
- 15 minutes: deliver the core demo

## Read before touching CI

- [Chromatic with GitHub Actions](https://www.chromatic.com/docs/github-actions/)
- [Chromatic access control](https://www.chromatic.com/docs/access/)

## Put the workflow on rails

1. Inspect the repository's Chromatic workflow. If the facilitator has not added it yet, create
   `.github/workflows/chromatic.yml` with:
   - checkout using `fetch-depth: 0`;
   - Node 20 and the pnpm version from `packageManager`;
   - `pnpm install --frozen-lockfile`;
   - the token from `${{ secrets.CHROMATIC_PROJECT_TOKEN }}`;
   - `workingDir: packages/react`, so the action uses that package's Storybook build script and config;
   - a `push` trigger for `main` and learner branches.
2. Do not enable automatic acceptance, `exitZeroOnChanges`, or TurboSnap for learner branches.
3. Push your branch and follow the action, Chromatic build, UI Tests, and UI Review checks from commit
   to result. Verify the build maps to the pushed commit rather than an ephemeral merge commit.
4. Ask the facilitator before changing branch protection or required checks. Reading and explaining
   policy is part of this day; changing repository-wide policy is not.
5. Deliver the five-minute core demo below to a teammate or record it for asynchronous feedback.

## Run the five-minute version

1. State Trailhead Travel's inconsistent UI-testing problem.
2. Show an editable React story and its Autodocs.
3. Switch among deterministic MSW states.
4. Run an interaction and accessibility test.
5. Open a Chromatic diff, explain the baseline, and show the passing CI result.
6. Close with a practical adoption next step.

## Check the handoff from Git to Chromatic

- The workflow uses full Git history and a repository secret.
- The branch build is attached to the expected commit and surfaces its checks.
- Your progress file contains feedback on the five-minute demo and one improvement for the capstone.

## Be ready for the ancestry question

Be ready when someone asks: **Why do we recommend building `main`, and why are push-based workflows
often safer for baseline ancestry than default pull-request merge commits?**

<details>
<summary>Recovery</summary>

If the secret is unavailable on your branch, do not paste it into the workflow. Confirm repository
permissions and secret scope with the facilitator. External forks intentionally do not receive normal
repository secrets.

</details>

## Mark the core path complete

```sh
git add .github/workflows/chromatic.yml progress/<handle>.md
git commit -m "onboarding(day-10): automate Chromatic and complete core demo"
```

You have completed the required core. Continue into the advanced path when composition, scale, and
optimization are relevant to the customer conversation.

</CoursePage>
